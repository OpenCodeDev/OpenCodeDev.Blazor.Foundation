@using System.Text;
<code class="@(("code-block") + (Class != null ? Class : ""))" @attributes="AdditionalAttributes">@((Content != null ? Encoding.UTF8.GetString(Convert.FromBase64String(Content)) : ""))@ChildContent</code>

@code {

}
